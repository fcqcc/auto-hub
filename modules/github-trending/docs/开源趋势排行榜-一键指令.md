# 开源趋势排行榜（GitHub & Gitee）— 一键指令

> **使用说明**：复制「启动语」，粘贴到任意 AI，AI 会按流程引导你生成完整项目。
> 参考源码：workspace/github-trending/（scraper.py + app.py + templates/index.html）

---

## 基本信息

| 项目 | 内容 |
|------|------|
| **项目名** | 开源趋势排行榜 |
| **任务类型** | 爬虫型 + API 调用型（混合） |
| **核心技术栈** | `requests` + `BeautifulSoup` + `Flask` + `HTML/CSS` |
| **运行环境** | Windows/Mac/Linux 桌面 |
| **网络适配** | GitHub 直连；Gitee 走官方 API；pip 走清华镜像 |
| **数据源** | GitHub Trending（爬虫）+ Gitee（Search API）+ GitHub Search API（备用）|

---

## 页面效果（必须严格对标 src）

**双模式：📈 上升榜（按新增Star） / 🏆 总榜（按Star总数）**

```
┌─────────────────────────────────────────────────────────────────┐
│  🐙 开源趋势 · GitHub & Gitee          更新于 2026-06-11 08:00  │
├───────────┬─────────────────────────────────────────────────────┤
│ 🏆 排行   │  [📈 上升榜 · 按新增 Star] [🏆 总榜 · 按 Star 总数] │
│   TOP 20  │  [📅 今日] [📆 本周] [📊 本月]                      │
│           │  📈 上升榜 · 按今日新增 Star 排名                    │
│ 按新增Star│  [📋 全部] [🤖 AI/ML 8] [🔧 开发工具 5] ...         │
│ · 今日    │                                                      │
│           │  ┌── 统计卡 ──┐ ┌── 统计卡 ──┐ ┌── 统计卡 ──┐      │
│  #1 repo  │  │ 📦 28 款  │ │ 🔥 AI/ML  │ │ 🌐 22 · 6  │      │
│  #2 repo  │  └──────────┘ └──────────┘ └──────────┘      │
│  ...      │                                                      │
│           │  ┌─ GitHub ──────────────────────┐                   │
│           │  │ 📈 上升榜                     │                   │
│           │  │ 共 22 项 · 按今日新增 Star 排序│                   │
│           │  ├────────────────────────────────┤                   │
│           │  │ ▲ +1.2k    ┌─ 卡片 ────────┐  │                   │
│           │  │ openai/whisper [🤖 AI/ML]  │  │                   │
│           │  │ Speech recognition...      │  │                   │
│           │  │ ● Python ⭐ 72.3k 🍴15.2k  │  │                   │
│           │  │ ▓▓▓▓▓▓▓░░░░░ (增长进度条)  │  │                   │
│           │  └────────────────────────────┘  │                   │
│           ├─ Gitee ─────────────────────────┤                   │
│           │  │ ▲ +890     ┌─ 卡片 ────────┐  │                   │
│           │  │ vuejs/core  [🌐 Web 框架]  │  │                   │
│           │  └────────────────────────────┘  │                   │
└───────────┴─────────────────────────────────────────────────────┘
```

---

## 启动语（复制这句）

```
我要搭一个开源趋势排行榜网页：GitHub+Gitee 双源、上升榜/总榜双模式、每日/每周/每月切换、12类分类筛选、暗色暖橙 UI。Flask 一键启动。
```

---

## AI 对话流程（6 步）

### 第 0 步：欢迎
```
你好！我来搭开源趋势排行榜。2 个问题搞定。
准备好了吗？我们开始 👇
```

### 第 1 步：问题 1/2（数据源）
```
问题 1/2：趋势数据从哪里获取？

A. GitHub + Gitee 双源（推荐，数据最全）
B. 仅 GitHub Trending
C. 仅 Gitee（国内网络最佳）
D. 不知道（你帮我选）

你选哪个？
（A 数据最全，零注册，国内直连）
```
> **推荐 A（双源）**

### 第 2 步：问题 2/2（功能偏好）
```
问题 2/2：默认展示哪种模式？

A. 上升榜（按新增 Star，默认，推荐）
B. 总榜（按 Star 总数）
C. 双模式都可以（带切换按钮）
D. 不过滤，全部展示

你选哪个？
（推荐 C，功能最完整）
```
> **推荐 C（双模式可切换）**

### 第 3 步：确认
```
好的，总结一下：
- 数据源：[A.双源 / B.仅GitHub / C.仅Gitee]
- 默认模式：[C.双模式可切换]

确认没问题，回「确认」，我开始生成。
```

### 第 4 步：生成项目包
```
github-trending/
├── scraper.py           # 数据抓取 + 缓存 + 分类 + 翻译
├── app.py               # Flask 后端（路由 /trending/api/...）
├── templates/
│   └── index.html       # 暗色暖橙 UI（侧边栏+卡片+统计+双模式）
├── requirements.txt     # 依赖
├── run.bat              # Windows 一键启动
└── run.sh               # Mac/Linux 启动
```

#### ===== scraper.py 规范 =====

```python
"""
GitHub + Gitee 趋势爬虫
"""

import requests
from bs4 import BeautifulSoup
import json, os, re, time
from datetime import datetime, timedelta
from pathlib import Path

CACHE_DIR = Path(__file__).parent / 'cache'
CACHE_DIR.mkdir(exist_ok=True)
CACHE_TTL = 300  # 5分钟
MAX_RETRIES = 2
HEADERS = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'}

# 缓存工具函数
def _extract_number(text):  # 支持 '821 stars today' 和 '1.2k'
def _load_cache(key):       # 读取缓存，过期→None
def _load_stale_cache(key): # 兜底：返回过期缓存而非空
def _save_cache(key, items):# 写入缓存

# ─── GitHub Trending（爬虫）───
def scrape_github_trending(since='daily') -> list[dict]:
    """抓取 github.com/trending，返回 [{name, description, language, stars, forks, growth, source, url}]"""
    # 2次重试 + 兜底过期缓存
    # BeautifulSoup 解析 article.Box-row
    ...

# ─── Gitee Trending（官方API）───
def scrape_gitee_trending() -> list[dict]:
    """调 gitee.com/api/v5/search/repos，返回同结构列表"""
    ...

# ─── GitHub Search API 备用 ───
def scrape_github_api(since='daily') -> list[dict]:
    """当爬虫失败时用 search/repositories 兜底"""
    ...

# ─── 抓取入口 ───
def get_all_trending(since='daily') -> dict:
    """主入口，返回 {github: [...], gitee: [...], categories: [...], updated_at: '2026-06-11 08:00'}"""
    gh = scrape_github_trending(since) or scrape_github_api(since) or []
    gt = scrape_gitee_trending() or []
    gh = apply_classification(gh)
    gt = apply_classification(gt)
    categories = collect_categories(gh + gt)
    # 翻译
    gh = translate_descriptions(gh)
    gt = translate_descriptions(gt)
    return {
        'github': gh, 'gitee': gt,
        'categories': categories,
        'updated_at': datetime.now().strftime('%Y-%m-%d %H:%M'),
    }

# ─── 分类系统（12类，与 src 完全一致）───
CATEGORY_RULES = [...]  # 12 类关键词，同上版不变
CATEGORY_META = {...}   # label/color/emoji

def classify_repo(repo) -> str:
def apply_classification(repos) -> list:
def collect_categories(repos) -> list[dict]:

# ─── 描述翻译（DashScope + 缓存）───
TRANS_CACHE = CACHE_DIR / 'translations.json'
def _load_trans_cache() -> dict:
def _is_english(text) -> bool:    # ASCII占比>30% → 需要翻译
def _get_dashscope_key() -> str:  # 环境变量 → .env → /opt/auto-daily/.env
def translate_descriptions(repos) -> list:
    """批量翻译英文描述，写入 repo['description_zh']。无Key就跳过。"""
```

> AI 注意：翻译是锦上添花功能。无 DashScope Key 时不翻译，不影响核心功能。如果不要翻译，把 `translate_descriptions` 调用去掉。

#### ===== app.py 规范 =====

```python
from flask import Flask, render_template, jsonify
from scraper import get_all_trending

app = Flask(__name__)

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/trending/api/trending/<since>')
def api_trending(since):
    if since not in ('daily', 'weekly', 'monthly'):
        since = 'daily'
    return jsonify(get_all_trending(since))

@app.route('/trending/api/trending')
def api_trending_default():
    return api_trending('daily')

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5051, debug=True)
```

> 注意路由路径是 `/trending/api/trending/`，不是 `/api/trending/`。

#### ===== templates/index.html 规范 =====

**颜色（暖橙风 #E97F4A）：**
```css
:root {
  --bg-body: #0a0e14; --bg-sidebar: #0f1319;
  --bg-card: rgba(22,28,36,0.85); --bg-card-hover: rgba(28,35,45,0.95);
  --border: rgba(48,60,75,0.4); --border-hover: rgba(233,127,74,0.25);
  --accent: #E97F4A; --accent-glow: rgba(233,127,74,0.15); --accent-muted: rgba(233,127,74,0.08);
  --text-primary: #e6edf3; --text-secondary: #8b949e; --text-muted: #484f58;
  --green: #3fb950; --green-glow: rgba(63,185,80,0.15); --red: #f85149;
  --radius: 12px; --radius-sm: 8px; --sidebar-w: 300px; --header-h: 56px;
}
```

**页面结构（以下功能点全部必须实现，对标 src 源码）：**

1. **顶部栏（header）**：fixed，GitHub logo SVG + 标题「开源趋势 · GitHub & Gitee」+ `update-info`（更新于 XX）
2. **双栏布局**：sidebar + main（flex）
3. **侧边栏（sidebar）**：标题「🏆 排行 TOP 20」+ 副标题（按新增Star降序·今日）+ 列表。每项：排名(top3橙色)、来源标记(GH/GT)、仓库名、语言色点、增长/总分徽章
4. **模式切换（switch-bar #switchBar）**：2个按钮 `📈 上升榜 · 按新增Star` / `🏆 总榜 · 按Star总数`。active=accent背景
5. **时间切换（switch-bar #timeBar）**：3个按钮 `📅 今日` / `📆 本周` / `📊 本月`。总榜模式时隐藏
6. **模式说明条（mode-bar）**：动态文案（「📈 今日上升榜 · 按今日新增Star排序」「🏆 总榜 · 按Star总数排序」）
7. **分类筛选（category-bar）**：pill 按钮，从 API 返回的 `categories` 动态生成，active=accent
8. **统计卡（stats-row）**：3张卡——📦收录仓库总数 / 🔥最热门分类 / 🌐 GitHub · Gitee 数量
9. **卡片网格（card-grid）**：minmax(340px,1fr) 自适应
10. **卡片（trending-card）**：顶部渐变线 → 右上角增长徽章(绿/红/金) → 仓库名+分类标签(颜色自适应) → 描述(description_zh优先) → meta(语言色点+名+⭐star+🍴fork+来源) → 增长进度条(growth bar)
11. **GitHub/Gitee 分区域渲染**：section-header 区分（GitHub 用 GH logo SVG，Gitee 用红色圆点 SVG）
12. **前端排序+筛选**：全部在前端完成（JS 内 `filterAndSort` 函数），根据 mode + time + category 动态过滤
13. **自动刷新**：`setInterval(fetchData, 300000)` 每5分钟
14. **LANG_COLORS 映射**：JavaScript/Python/TypeScript/Go/Rust/C++/HTML/CSS/Vue/Shell/Swift/Kotlin/Ruby/Dart/Scala + Default
15. **加载/空/错误状态**：spinner / empty-state / error-state
16. **响应式 ≤768px**：隐藏侧边栏、卡片1列、switch-bar 100%宽度

**JS 核心变量：**
```js
let currentMode = 'growth';  // 'growth' | 'alltime'
let currentSince = 'daily';  // 'daily' | 'weekly' | 'monthly'
let currentCategory = 'all';
let currentData = null;
```

**API 调用：** `fetch('/trending/api/trending/${currentSince}')`，返回 `{github: [...], gitee: [...], categories: [...], updated_at: '...'}`

#### ===== requirements.txt =====
```
flask>=3.0
requests>=2.28
beautifulsoup4>=4.11
```

#### ===== run.bat（Windows）=====
```batch
@echo off
chcp 65001 >nul
echo 🚀 启动开源趋势排行榜...
cd /d "%~dp0"
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple -q
python app.py
pause
```

#### ===== run.sh（Mac/Linux）=====
```bash
#!/bin/bash
cd "$(dirname "$0")"
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple -q
python3 app.py
```

### 第 5 步：部署 + 自动启动
```
项目生成好了！接下来：
A. 让我帮你启动（装依赖+启动+展示地址）
B. 我自己启动
C. 先看说明
```
**选 A** → AI 自动：
```
✅ 启动成功！
👉 访问地址：http://localhost:5051
👉 API：http://localhost:5051/trending/api/trending/daily
```

### 第 6 步：完成
```
🎉 搞定！访问 http://localhost:5051 看效果。

页面功能：
├── 🔀 双模式：上升榜（新增Star）/ 总榜（Star总数）
├── ⏱ 时间切换：今日 / 本周 / 本月
├── 🏷️ 分类筛选：12类（AI/ML、开发工具、Web...）
├── 📋 侧边栏：TOP20 排行 + 徽章
├── 📊 统计卡：总数 / 热门分类 / 双源分布
├── 🃏 卡片：名称+标签+描述+语言+Star+Fork+增长条
├── 🌍 双源分区：GitHub / Gitee 各自区域
├── 🎨 暗色暖橙主题（#E97F4A）
└── 📱 响应式 + 自动刷新5分钟
```

---

## 验证清单

- [ ] API 返回 `{github: [...], gitee: [...], categories: [...], updated_at: '...'}`
- [ ] 双模式切换（上升榜/总榜）功能正常
- [ ] 时间切换（daily/weekly/monthly）功能正常
- [ ] 分类 pill 筛选（前端过滤）功能正常
- [ ] 统计卡 3 张数据正确
- [ ] 侧边栏 TOP20（按当前模式排序）
- [ ] 卡片含增长徽章（颜色区分绿/红/金）
- [ ] 卡片含增长进度条（上升榜模式显示）
- [ ] GitHub/Gitee 分区域展示
- [ ] 缓存系统（5min TTL + 过期兜底）
- [ ] 抓取失败时返回过期缓存而非空
- [ ] 暗色暖橙主题（#0a0e14 背景，#E97F4A accent）
- [ ] 加载 spinner + 空状态 + 错误状态
- [ ] 响应式 ≤768px
- [ ] pip 默认清华镜像
- [ ] 路由路径 `/trending/api/trending/`
